# Artifacts from an early discussion with Claude. Not deep, just to get a project idea and explore it before committing.
claude-initial-design.png
log-intelligence-design-summary.md
Initial notes and plan.md

# Used for complex architecture reviews by Claude, to keep the context in an external file and be able to restart a new session with relevant context
doc/design/reviews

# I use Obsidian to manage the documentation within this repo
.obsidian

.idea

# Gradle
.gradle
build/

# Node / Angular
node_modules
angular/out-tsc
angular/.angular/

# Angular build output (generated into Ktor resources, not committed)
ingestion-service/src/main/resources/static

# Claude Code struggling mightily to reliably use Gradle in Windows because it's trying random bash scripts,
# so helping it a bit with an explicit environment flag.
.devenv
